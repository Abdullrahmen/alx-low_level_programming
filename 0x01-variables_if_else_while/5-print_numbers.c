#include <stdio.h>

/**
* main - Main function
* Description: Main function
* Return: Always 0
*/

int main(void)
{
	int n;

	for (n = 0; n < 10; ++n)
		printf("%d", n);

	putchar(10);
	return (0);
}
